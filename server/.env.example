# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/instamarketing

# JWT Secret (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Meta/Facebook App Credentials (Get from https://developers.facebook.com)
META_APP_ID=your_meta_app_id
META_APP_SECRET=your_meta_app_secret

# Instagram Graph API
INSTAGRAM_ACCESS_TOKEN=your_instagram_access_token
INSTAGRAM_BUSINESS_ACCOUNT_ID=your_instagram_business_account_id

# File Upload Settings
MAX_FILE_SIZE=50000000
UPLOAD_PATH=./uploads

# ============================================================
# AI VIDEO GENERATION (Required for video/reel creation)
# ============================================================

# Replicate API Token (Get from https://replicate.com/account/api-tokens)
# Required for: AI video generation (text-to-video, image-to-video)
# Pricing: ~$0.25-0.50 per video, requires billing setup at replicate.com/account/billing
REPLICATE_API_TOKEN=your_replicate_api_token

# OpenAI API Key (Get from https://platform.openai.com/api-keys)
# Required for: Caption generation, hashtags, scripts, content ideas, AI chat
OPENAI_API_KEY=your_openai_api_key

# ============================================================
# OPTIONAL: Google Cloud TTS (for voiceover generation)
# ============================================================
# Google Cloud Text-to-Speech (set path to your service account JSON)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ============================================================
# OPTIONAL: Cloudinary (for media storage)
# ============================================================
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret
